/**
 * Message on rejected promises when @anthropic-ai/claude-agent-sdk closes a query with
 * pending MCP/control requests (see Query.cleanup in the SDK).
 */
export const SDK_QUERY_CLOSED_MESSAGE = 'Query closed before response received';
